# WireGuard UDP Relay Configuration
# Copy this file to .env and configure with your settings

# Comma-separated list of ports to listen on
# Example: 51820,443
LISTEN_PORTS=51820,443

# Target WireGuard endpoint DDNS URL (without port)
# Example: xxxxxxx.glddns.com
ENDPOINT_DDNS=xxxxxxx.glddns.com

# Target WireGuard endpoint port
ENDPOINT_PORT=58120

# DNS resolution check interval
# How often to check if the DDNS endpoint IP has changed
# Format: duration string (e.g., 5m, 10m, 1h)
# Default: 5m
DNS_CHECK_INTERVAL=5m
